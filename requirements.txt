mcp>=1.2.0
pyyaml>=6.0
# remote hosting (app.py — Streamable-HTTP MCP + REST on Azure):
uvicorn>=0.27
starlette>=0.37
# optional: only web/extract_server.py (auto-fill helper) needs these:
anthropic>=0.40.0
python-dotenv>=1.0.0
